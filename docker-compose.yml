# Golden Pro CRM — single always-on container (frontend + backend + SQLite).
#
# Local/single-container fallback only. Public VPS production must use
# deploy/docker-compose.yml through scripts/deploy-vps.ps1 so Caddy owns the
# trusted client-IP boundary. SQLite and the WhatsApp session use named volumes.
#
#   cp .env.production.example .env.production   # then fill secrets
#   docker compose --env-file .env.production up -d --build
#   docker compose --env-file .env.production logs -f
services:
  crm:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_COMMIT: ${BUILD_COMMIT:-unknown}
        VITE_DATA_PROVIDER: ${VITE_DATA_PROVIDER:-sqlite}
        VITE_DB_PROVIDER: ${VITE_DB_PROVIDER:-sqlite}
        VITE_LOCAL_AUTH: ${VITE_LOCAL_AUTH:-false}
        VITE_PUBLIC_CONTACT_PHONE: ${VITE_PUBLIC_CONTACT_PHONE:?Set VITE_PUBLIC_CONTACT_PHONE in .env.production}
    image: golden-pro-crm:latest
    container_name: golden-pro-crm
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      BUILD_COMMIT: ${BUILD_COMMIT:-unknown}
      # This Compose file has no bundled trusted proxy. Override the production
      # example so a direct client can never spoof x-breexe-client-ip.
      TRUST_PROXY_HEADERS: "false"
      PORT: "8080"
      DB_PATH: /app/.runtime/golden-crm.db
      WA_SESSION_DIR: /app/.wa-session
    ports:
      - "127.0.0.1:3000:8080"  # loopback only; use the bundled deploy stack publicly
    volumes:
      - crm-data:/app/.runtime       # SQLite database (calls, departments, outbox)
      - crm-wa:/app/.wa-session       # WhatsApp linked-device session (scan QR once)
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8080/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      start_period: 25s
      retries: 3

volumes:
  crm-data:
  crm-wa:
